# `awsinfo iam roles [substrings]*`

Lists all IAM roles in the current account and region. 

If `substrings` is given it will only print iam roles that contain all `substrings` in the `Name` or `Id`.