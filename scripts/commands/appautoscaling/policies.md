# `awsinfo appautoscaling policies NAMESPACE -- [filters]*`

List all Application Autoscaling Policies for a namespace.

The namespace has to fit one of the supported namespaces, e.g. `ecs`

## Filter matches against

* ScalableDimension 
* ResourceId 