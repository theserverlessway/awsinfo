# `awsinfo appsync resolvers [api-filter]* -- [type-filter]*`

List all field resolvers for a specific type (including Mutation, Query or Subscription)

## First filter matches against

* Name
* API Id
* AuthenticationType
* GrapQL Uri

## Second filter matches against

* Type Name
