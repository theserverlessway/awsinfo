# `awsinfo commands [service]`

If no specific service is given as an argument it will list all available services. 
If you specify a service as the first parameter it will list all commands for that service.